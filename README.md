# sass-use-forward-check
Scratchpad for SASS namespacing experimentation

1. Install dependencies
> npm install

2. Compile assets in watch mode
> npm run sass

3. Generated CSS built to root `dev` directory.
